{
	"name": "pi-approval-modes",
	"version": "1.0.0",
	"description": "Approval and shell guard modes for pi",
	"keywords": [
		"pi-package"
	],
	"type": "module",
	"main": "src/index.ts",
	"pi": {
		"extensions": [
			"src/index.ts"
		]
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/m62624/pi-approval-modes.git"
	},
	"license": "MIT",
	"scripts": {
		"build": "tsc",
		"check": "biome check .",
		"format": "biome format --write .",
		"test": "vitest run"
	},
	"dependencies": {
		"@earendil-works/pi-coding-agent": "latest"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.4.15",
		"@types/node": "^24.0.0",
		"typescript": "^6.0.3",
		"vitest": "^4.1.6"
	}
}
